# Enforce LF line endings for all text files — prevents false "modified" status on Windows
* text=auto eol=lf

# Enforce LF for shell scripts explicitly
*.sh text eol=lf
AGENT-PLATFORM-TEMPLATES/**/*.sh text eol=lf
