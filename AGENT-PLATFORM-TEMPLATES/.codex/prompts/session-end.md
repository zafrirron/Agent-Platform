# Codex — session end

**User command:** `Read .codex/prompts/session-end.md and execute it.`

My framework folder name is: `codex`

Do not edit `.claude/`, `.cursor/`, `.agents/`, or `.opencode/` during this session.

Read `.agent/session-end-shared.md` and execute it, replacing every `<fw>` with `codex`.
