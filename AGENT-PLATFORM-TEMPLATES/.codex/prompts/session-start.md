# Codex (VS Code) — session start

**User command:** `Read .codex/prompts/session-start.md and execute it.`

## Steps

1. `SYNC-POINTS.md`, `.codex/instructions.md`, `.agent/SYNC.md`, registry, CURRENT.md
2. Optional: `node .agent/tools/check_locks.js codex <files>`
3. Conflict check vs other frameworks
4. Set `frameworks.codex` → `active`; `meta.updated_by: codex`
5. Log `Framework: codex` in CURRENT.md
6. Expert from `.agent/agents/` per `AGENTS.md`

Do not edit `.cursor/`, `.claude/`, `.agents/`.

Proceed.
