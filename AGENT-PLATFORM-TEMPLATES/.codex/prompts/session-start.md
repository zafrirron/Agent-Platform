# Codex — session start

**User command:** `Read .codex/prompts/session-start.md and execute it.`

My framework folder name is: `codex`

Do not edit `.claude/`, `.cursor/`, `.agents/`, or `.opencode/` during this session.

Read `.agent/session-start-shared.md` and execute it, replacing every `<fw>` with `codex`.
