# Cursor — session end

1. Summarize: files, behavior, tests, blockers
2. Set `frameworks.cursor` → `idle`, clear `files`; update `meta.updated_by: cursor`
3. Update your `CURRENT.md` entry → `done` | `blocked` + **Next agent**
4. Optional: `node .agent/tools/prune_handoff.js` if log is long
5. Tell user next IDE start command from `SYNC-POINTS.md`

Do not edit other frameworks' private folders.
