# Cursor — session start

**User command:** `Read .cursor/prompts/session-start.md and execute it.`

My framework folder name is: `cursor`

Do not edit `.claude/`, `.agents/`, `.codex/`, or `.opencode/` during this session.

Read `.agent/session-start-shared.md` and execute it, replacing every `<fw>` with `cursor`.
