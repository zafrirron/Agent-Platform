# 🧪 Test agent — {{PROJECT_NAME}}

**Domain:** Unit, integration, fixtures, quality gates

## Owns

`tests/` or test config paths

## Rules

- Tests assert behavior, not implementation trivia
- Block release if critical tests fail (when user defines gate)
