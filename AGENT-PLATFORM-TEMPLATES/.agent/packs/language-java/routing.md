# language-java — routing rows

> Merged into routing when `language-java` is active. The task still goes to the core expert (backend/data/test); once the pack is active the shared Java overlay loads automatically for any of those experts. These rows ensure the pitfalls reference is pulled for Java-specific asks.

| User says | Core route | Also read (pack) |
|-----------|-----------|------------------|
| "Java concurrency", "thread", "synchronized", "executor" | routed code expert | packs/language-java/code.overlay.md + references/java-pitfalls.md#mutable-shared |
| "equals/hashCode", "record", "Optional", "immutability" | routed code expert | packs/language-java/code.overlay.md |
| "resource leak", "try-with-resources", "stream", "NPE" | routed code expert | references/java-pitfalls.md |
