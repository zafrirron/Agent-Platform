# language-typescript — routing rows

> Merged into routing when `language-typescript` is active. The task still goes to the core expert (backend/frontend/data/test); once the pack is active the shared TS overlay loads automatically for any of those experts. These rows ensure the pitfalls reference is pulled for TS-specific asks.

| User says | Core route | Also read (pack) |
|-----------|-----------|------------------|
| "TypeScript type", "generic", "`any`/`unknown`", "type error" | routed code expert | packs/language-typescript/code.overlay.md + references/typescript-pitfalls.md |
| "tsconfig", "strict mode", "type narrowing", "discriminated union" | routed code expert | packs/language-typescript/code.overlay.md |
| "validate API response", "parse JSON safely", "zod schema" | routed code expert | references/typescript-pitfalls.md#assertion-vs-validation |
