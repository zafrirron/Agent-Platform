# language-cpp — routing rows

> Merged into routing when `language-cpp` is active. The task still goes to the core expert (backend/test); once the pack is active the shared C++ overlay loads automatically for those experts. These rows ensure the pitfalls reference is pulled for C++-specific asks.

| User says | Core route | Also read (pack) |
|-----------|-----------|------------------|
| "memory", "smart pointer", "ownership", "leak", "RAII" | routed code expert | packs/language-cpp/code.overlay.md + references/cpp-pitfalls.md |
| "move semantics", "rule of 5", "copy/move ctor", "dangling" | routed code expert | packs/language-cpp/code.overlay.md |
| "data race", "undefined behavior", "UB", "std::mutex" | routed code expert | references/cpp-pitfalls.md#data-race |
