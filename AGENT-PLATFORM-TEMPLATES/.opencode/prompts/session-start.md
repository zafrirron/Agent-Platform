# OpenCode — session start

**User command:** `Read .opencode/prompts/session-start.md and execute it.`

My framework folder name is: `opencode`

Do not edit `.claude/`, `.cursor/`, `.agents/`, or `.codex/` during this session.

Read `.agent/session-start-shared.md` and execute it, replacing every `<fw>` with `opencode`.
