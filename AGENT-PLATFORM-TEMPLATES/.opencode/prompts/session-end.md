# OpenCode — session end

**User command:** `Read .opencode/prompts/session-end.md and execute it.`

My framework folder name is: `opencode`

Do not edit `.claude/`, `.cursor/`, `.agents/`, or `.codex/` during this session.

Read `.agent/session-end-shared.md` and execute it, replacing every `<fw>` with `opencode`.
