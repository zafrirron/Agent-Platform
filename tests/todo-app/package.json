{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "A simple todo REST API — platform E2E test project",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --coverage"
  },
  "dependencies": {
    "express": "^4.18.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "supertest": "^6.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
